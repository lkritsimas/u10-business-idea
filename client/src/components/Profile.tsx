import React from 'react';
import { Typography } from '@material-ui/core';

export const Profile: React.FC = () => (
  <div>
    <Typography>Placeholder for profile component</Typography>
  </div>
);

export default Profile;
