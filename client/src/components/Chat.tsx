import React from 'react';
import { Typography } from '@material-ui/core';

export const Chat: React.FC = () => (
  <div>
    <Typography>Placeholder for chat component </Typography>
  </div>
);

export default Chat;
